import { createTheme } from "@mui/material/styles";
export const theme = createTheme({
  palette: {
    primary: {
      main: "#000000",
      light: "#D4D3D4",
      dark: "#292929",
      contrastText: "#BA0C2E",
    },
  },
});

export default theme;
